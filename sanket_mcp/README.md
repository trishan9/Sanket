# sanket_mcp

The twelve tool schemas over MCP (Phase 14).

Named `sanket_mcp` rather than `mcp` because a top-level `mcp/` package would shadow the
installed `mcp` distribution that the server itself depends on.
