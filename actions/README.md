# actions

Consequence. Without this layer the system is a very good assistant.

- `board.py` — `write_status` is the autonomous consequence. It refuses any level above
  `WATCH`: raising public status to `ALERT` requires a named district officer, and the
  refusal is enforced in code rather than left to a prompt.

Voice, SMS, WhatsApp and the gate arrive in Phase 10 behind a single `Channel` interface,
so the messaging vendor is one adapter rather than a dependency of the whole system.
